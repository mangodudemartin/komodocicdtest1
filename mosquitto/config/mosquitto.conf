# Eclipse Mosquitto MQTT Broker Configuration

listener 1883
# WARNING: Anonymous access is enabled for POC convenience.
# In production, disable this and configure TLS + password_file or an ACL.
allow_anonymous true

# Persistence
persistence true
persistence_location /mosquitto/data/

# Logging
log_dest stdout
log_type all
